# id: string,
# designacao: string,
# subtotal: number,
# horas_estimadas: number,
# id_prestador: string,
# id_servico: string,
# preco_hora: number,
# estado: string,
# id_orcamento: string,
# enabled: boolean,
# created_at: string,
# updated_at: string

components:
  schemas:
    PrestacaoServico:
      type: object
      properties:
        id:
          type: string
          example: "1"
        designacao:
          type: string
          example: "Prestacao de servico"
        subtotal:
          type: number
          example: 100
        horas_estimadas:
          type: number
          example: 10
        id_prestador:
          type: string
          example: "1"
        id_servico:
          type: string
          example: "1"
        preco_hora:
          type: number
          example: 100
        estado:
          type: string
          example: "pendente"
        id_orcamento:
          type: string
          example: "1"
        enabled:
          type: boolean
          example: true
        created_at:
          type: string
          example: "2022-01-01T00:00:00.000Z"
        updated_at:
          type: string
          example: "2022-01-01T00:00:00.000Z"

    CreatePrestacaoServico:
      type: object
      properties:
        designacao:
          type: string
          example: "Prestacao de servico"
        subtotal:
          type: number
          example: 100
        horas_estimadas:
          type: number
          example: 10
        id_prestador:
          type: string
          example: "1"
        id_servico:
          type: string
          example: "1"
        preco_hora:
          type: number
          example: 100
        estado:
          type: string
          example: "pendente"
        id_orcamento:
          type: string
          example: "1"
        enabled:
          type: boolean
          example: true

    UpdatePrestacaoServico:
      type: object
      properties:
        designacao:
          type: string
          example: "Prestacao de servico"
        subtotal:
          type: number
          example: 100
        horas_estimadas:
          type: number
          example: 10
        id_prestador:
          type: string
          example: "1"
        id_servico:
          type: string
          example: "1"
        preco_hora:
          type: number
          example: 100
        estado:
          type: string
          example: "pendente"
        id_orcamento:
          type: string
          example: "1"
        enabled:
          type: boolean
          example: true
